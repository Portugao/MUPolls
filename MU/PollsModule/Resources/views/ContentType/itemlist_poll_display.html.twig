{# Purpose of this template: Display polls within an external context #}
{% for poll in items %}
    <h3>{{ poll.getTitleFromDisplayPattern() }}</h3>
    <p><a href="{{ path('mupollsmodule_poll_display', { 'id': poll.id }) }}">{{ __('Read more') }}</a>
    </p>
{% endfor %}
