{# Purpose of this template: Display one certain option within an external context #}
<div id="option{{ option.getKey() }}" class="mupollsmodule-external-option">
{% if displayMode == 'link' %}
    <p class="mupollsmodule-external-link">
    <a href="{{ path('mupollsmodule_option_display', {'id': option.getId()}) }}" title="{{ option|mupollsmodule_formattedTitle|e('html_attr') }}">
    {{ option|mupollsmodule_formattedTitle|notifyFilters('polls.filter_hooks.options.filter')|safeHtml }}
    </a>
    </p>
{% endif %}
{% if hasPermission('MUPollsModule::', '::', 'ACCESS_EDIT') %}
    {# for normal users without edit permission show only the actual file per default #}
    {% if displayMode == 'embed' %}
        <p class="mupollsmodule-external-title">
            <strong>{{ option|mupollsmodule_formattedTitle|notifyFilters('polls.filter_hooks.options.filter')|safeHtml }}</strong>
        </p>
    {% endif %}
{% endif %}

{% if displayMode == 'link' %}
{% elseif displayMode == 'embed' %}
    <div class="mupollsmodule-external-snippet">
        &nbsp;
    </div>

    {# you can distinguish the context like this: #}
    {# % if source == 'block' %}
        ... detail block
    {% elseif source == 'contentType' %}
        ... detail content type
    {% elseif source == 'scribite' %}
        ...
    {% endif % #}

    {# you can enable more details about the item: #}
    {#
        <p class="mupollsmodule-external-description">
            {% if option.title is not empty %}{{ option.title }}<br />{% endif %}
        </p>
    #}
{% endif %}
</div>
