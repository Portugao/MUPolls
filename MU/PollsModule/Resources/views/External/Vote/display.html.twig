{# Purpose of this template: Display one certain vote within an external context #}
<div id="vote{{ vote.getKey() }}" class="mupollsmodule-external-vote">
{% if displayMode == 'link' %}
    <p class="mupollsmodule-external-link">
    <a href="{{ path('mupollsmodule_vote_display', {'id': vote.getId()}) }}" title="{{ vote|mupollsmodule_formattedTitle|e('html_attr') }}">
    {{ vote|mupollsmodule_formattedTitle|notifyFilters('polls.filter_hooks.votes.filter')|safeHtml }}
    </a>
    </p>
{% endif %}
{% if hasPermission('MUPollsModule::', '::', 'ACCESS_EDIT') %}
    {# for normal users without edit permission show only the actual file per default #}
    {% if displayMode == 'embed' %}
        <p class="mupollsmodule-external-title">
            <strong>{{ vote|mupollsmodule_formattedTitle|notifyFilters('polls.filter_hooks.votes.filter')|safeHtml }}</strong>
        </p>
    {% endif %}
{% endif %}

{% if displayMode == 'link' %}
{% elseif displayMode == 'embed' %}
    <div class="mupollsmodule-external-snippet">
        &nbsp;
    </div>

    {# you can distinguish the context like this: #}
    {# % if source == 'block' %}
        ... detail block
    {% elseif source == 'contentType' %}
        ... detail content type
    {% elseif source == 'scribite' %}
        ...
    {% endif % #}

    {# you can enable more details about the item: #}
    {#
        <p class="mupollsmodule-external-description">
        </p>
    #}
{% endif %}
</div>
