{# purpose of this template: inclusion template for display of related polls or hook assignments #}
{#
    You can use the context variable to check for the context of this list:
        - 'display': list of related polls included in a display template
        - 'hookDisplayView': list of polls assigned using an UI hook (display/view template)
        - 'hookDisplayEdit': list of polls assigned using an UI hook (edit template)
        - 'hookDisplayDelete': list of polls assigned using an UI hook (delete template)
#}
{% set hasAdminPermission = hasPermission('MUPollsModule:Poll:', '::', 'ACCESS_EDIT') %}
{% set hasEditPermission = hasPermission('MUPollsModule:Poll:', '::', 'ACCESS_EDIT') %}
{% if context != 'display' %}
    <h3>{{ __('Assigned polls') }}</h3>
    {{ pageAddAsset('stylesheet', zasset('@MUPollsModule:css/style.css')) }}
    {{ pageAddAsset('stylesheet', asset('jquery-ui/themes/base/jquery-ui.min.css')) }}
    {{ pageAddAsset('stylesheet', asset('bootstrap-jqueryui/bootstrap-jqueryui.min.css')) }}
    {{ pageAddAsset('javascript', asset('jquery-ui/jquery-ui.min.js')) }}
    {{ pageAddAsset('javascript', asset('bootstrap-jqueryui/bootstrap-jqueryui.min.js')) }}
    {{ pageAddAsset('javascript', zasset('@MUPollsModule:js/MUPollsModule.js'), 99) }}
    {% if context == 'hookDisplayView' and hasEditPermission %}
        {% set entityNameTranslated = __('poll') %}
        {{ pageAddAsset('javascript', zasset('@MUPollsModule:js/MUPollsModule.HookAssignment.js'), 99) }}
        {{ pageAddAsset('javascript', zasset('@MUPollsModule:js/MUPollsModule.InlineEditing.js'), 99) }}
        {{ pageAddAsset('javascript', zasset('@MUPollsModule:js/MUPollsModule.AutoCompletion.js'), 99) }}
    {% endif %}
{% endif %}
{% if noLink is not defined %}
    {% set noLink = false %}
{% endif %}
{% if items|default and items|length > 0 %}
<ul class="list-group mupollsmodule-related-item-list poll">
{% for item in items %}
    {% if hasAdminPermission or item.workflowState == 'approved' %}
    <li class="list-group-item">
<h4 class="list-group-item-heading">
{% spaceless %}
{% if not noLink %}
    <a href="{{ path('mupollsmodule_poll_' ~ routeArea ~ 'display', {'id': item.getId()}) }}" title="{{ item|mupollsmodule_formattedTitle|e('html_attr') }}">
{% endif %}
    {{ item|mupollsmodule_formattedTitle }}
{% if not noLink %}
    </a>
    <a id="pollItem{{ item.getKey() }}Display" href="{{ path('mupollsmodule_poll_' ~ routeArea ~ 'display', {'id': item.getId(), raw: 1}) }}" title="{{ __('Open quick view window') }}" class="mupolls-inline-window hidden" data-modal-title="{{ item|mupollsmodule_formattedTitle|e('html_attr') }}"><span class="fa fa-id-card-o"></span></a>
{% endif %}
{% endspaceless %}
</h4>
{% if context == 'hookDisplayView' and hasEditPermission %}
    {% set assignmentId = '' %}
    {% for assignment in assignments if assignment.getAssignedId() == item.getKey() %}
        {% set assignmentId = assignment.getId() %}
    {% endfor %}
    <p class="list-group-item-text">
        <a href="javascript:void(0);" title="{{ __f('Detach this %name%', {'%name%': entityNameTranslated})|e('html_attr') }}" class="detach-mupollsmodule-object hidden" data-assignment-id="{{ assignmentId|e('html_attr') }}"><i class="fa fa-chain-broken"></i> {{ __f('Detach %name%', {'%name%': entityNameTranslated}) }}</a>
    </p>
{% endif %}
    </li>
    {% endif %}
{% endfor %}
</ul>
{% endif %}
{% if context == 'hookDisplayView' and hasEditPermission %}
    {% set idPrefix = 'hookAssignmentPoll' %}
    {% set addLinkText = __f('Attach %name%', {'%name%': entityNameTranslated}) %}
    <div id="{{ idPrefix }}LiveSearch" class="mupollsmodule-add-hook-assignment">
        <a id="{{ idPrefix }}AddLink" href="javascript:void(0);" title="{{ addLinkText|e('html_attr') }}" class="attach-mupollsmodule-object hidden" data-owner="{{ subscriberOwner|e('html_attr') }}" data-area-id="{{ subscriberAreaId|e('html_attr') }}" data-object-id="{{ subscriberObjectId|e('html_attr') }}" data-url="{{ subscriberUrl|e('html_attr') }}" data-assigned-entity="poll"><i class="fa fa-link"></i> {{ addLinkText }}</a>
        <div id="{{ idPrefix }}AddFields" class="mupollsmodule-autocomplete">
            <label for="{{ idPrefix }}Selector">{{ __f('Find %name%', {'%name%': entityNameTranslated}) }}</label>
            <br />
            <i class="fa fa-search" title="{{ __f('Search %name%', {'%name%': entityNameTranslated})|e('html_attr') }}"></i>
            <input type="hidden" name="{{ idPrefix }}" id="{{ idPrefix }}" value="{% for assignment in assignments %}{% if not loop.first %},{% endif %}{{ assignment.getAssignedId() }}{% endfor %}" />
            <input type="hidden" name="{{ idPrefix }}Multiple" id="{{ idPrefix }}Multiple" value="0" />
            <input type="text" id="{{ idPrefix }}Selector" name="{{ idPrefix }}Selector" autocomplete="off" />
            <input type="button" id="{{ idPrefix }}SelectorDoCancel" name="{{ idPrefix }}SelectorDoCancel" value="{{ __('Cancel') }}" class="btn btn-default mupollsmodule-inline-button" />
            <a id="{{ idPrefix }}SelectorDoNew" href="{{ path('mupollsmodule_poll_' ~ routeArea ~ 'edit') }}" title="{{ __f('Create new %name%', {'%name%': entityNameTranslated}) }}" class="btn btn-default mupollsmodule-inline-button"><i class="fa fa-plus"></i> {{ __('Create') }}</a>
            <noscript><p>{{ __('This function requires JavaScript activated!') }}</p></noscript>
        </div>
    </div>
    {% set assignmentInitScript %}
        <script type="text/javascript">
        /* <![CDATA[ */
            var mUPollsInlineEditHandlers = [];
            var mUPollsEditHandler = {
                alias: '{{ idPrefix }}',
                prefix: '{{ idPrefix }}SelectorDoNew',
                moduleName: 'MUPollsModule',
                objectType: 'poll',
                inputType: 'autocomplete',
                windowInstanceId: null
            };
            mUPollsInlineEditHandlers.push(mUPollsEditHandler);

            mUPollsInitRelationHandling('poll', '{{ idPrefix }}', '{{ idPrefix }}SelectorDoNew', true, 'autocomplete', '{{ path('mupollsmodule_poll_' ~ routeArea ~ 'edit') }}');
        /* ]]> */
        </script>
    {% endset %}
    {{ pageAddAsset('footer', assignmentInitScript) }}
{% endif %}
