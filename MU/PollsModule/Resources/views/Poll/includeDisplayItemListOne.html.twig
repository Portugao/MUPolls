{# purpose of this template: inclusion template for display of related polls or hook assignments #}
{% set hasAdminPermission = hasPermission('MUPollsModule:Poll:', '::', 'ACCESS_COMMENT') %}
{% set hasEditPermission = hasPermission('MUPollsModule:Poll:', '::', 'ACCESS_COMMENT') %}
{% if noLink is not defined %}
    {% set noLink = false %}
{% endif %}
<h4>
{% spaceless %}
{% if not noLink %}
    <a href="{{ path('mupollsmodule_poll_' ~ routeArea ~ 'display', {'id': item.getId()}) }}" title="{{ item|mupollsmodule_formattedTitle|e('html_attr') }}">
{% endif %}
    {{ item|mupollsmodule_formattedTitle }}
{% if not noLink %}
    </a>
    <a id="pollItem{{ item.getKey() }}Display" href="{{ path('mupollsmodule_poll_' ~ routeArea ~ 'display', {'id': item.getId(), raw: 1}) }}" title="{{ __('Open quick view window') }}" class="mupolls-inline-window hidden" data-modal-title="{{ item|mupollsmodule_formattedTitle|e('html_attr') }}"><i class="fa fa-id-card-o"></i></a>
{% endif %}
{% endspaceless %}
</h4>
