{# purpose of this template: inclusion template for display of related polls or hook assignments #}
{% set hasAdminPermission = hasPermission('MUPollsModule:Poll:', '::', 'ACCESS_EDIT') %}
{% set hasEditPermission = hasPermission('MUPollsModule:Poll:', '::', 'ACCESS_EDIT') %}
{% if noLink is not defined %}
    {% set noLink = false %}
{% endif %}
<h4>
{% spaceless %}
{% if not noLink %}
    <a href="{{ path('mupollsmodule_poll_' ~ routeArea ~ 'display', {'id': item.getId()}) }}" title="{{ item|mupollsmodule_formattedTitle|e('html_attr') }}">
{% endif %}
    {{ item|mupollsmodule_formattedTitle }}
{% if not noLink %}
    </a>
    <a id="pollItem{{ item.getKey() }}Display" href="{{ path('mupollsmodule_poll_' ~ routeArea ~ 'display', {'id': item.getId(), raw: 1}) }}" title="{{ __('Open quick view window') }}" class="mupolls-inline-window hidden" data-modal-title="{{ item|mupollsmodule_formattedTitle|e('html_attr') }}"><span class="fa fa-id-card-o"></span></a>
{% endif %}
{% endspaceless %}
</h4>
